#!/usr/bin/env bash
set -euo pipefail

readonly SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"

# Keep the reduced GPT-2 diagnostic comparable to the baseline experiment.
export SCULPTOR_MESH_ROWS="${SCULPTOR_MESH_ROWS:-10}"
export SCULPTOR_MESH_COLS="${SCULPTOR_MESH_COLS:-10}"
export GPT2_NUM_DECODERS="${GPT2_NUM_DECODERS:-2}"
export GPT2_SEQUENCE_LENGTH="${GPT2_SEQUENCE_LENGTH:-6}"
export SCULPTOR_PARALLEL_WORKERS="${SCULPTOR_PARALLEL_WORKERS:-4}"
export SCULPTOR_DIGITAL_ISSUE_WIDTH="${SCULPTOR_DIGITAL_ISSUE_WIDTH:-2}"
export SCULPTOR_PLACEMENT_SCHEDULE="${SCULPTOR_PLACEMENT_SCHEDULE:-snake}"
export SCULPTOR_NETWORK_WORD_BITS="${SCULPTOR_NETWORK_WORD_BITS:-32}"
export SCULPTOR_LOCAL_MEMORY_BYTES_PER_CORE="${SCULPTOR_LOCAL_MEMORY_BYTES_PER_CORE:-8388608}"
export SCULPTOR_DUPLICATE_MATRICES="${SCULPTOR_DUPLICATE_MATRICES:-false}"
export SCULPTOR_REQUIRE_CHANGE="${SCULPTOR_REQUIRE_CHANGE:-true}"
export GOLEM_MODEL_MEMORY_BACKEND="${GOLEM_MODEL_MEMORY_BACKEND:-memhierarchy}"
export GOLEM_MODEL_PROFILE_MODE="${GOLEM_MODEL_PROFILE_MODE:-trace}"
export GOLEM_EXPERIMENT_OUTPUT_DIR="${GOLEM_EXPERIMENT_OUTPUT_DIR:-${SCRIPT_DIR}/results/gpt2/two-decoder-10x10}"

exec "${SCRIPT_DIR}/runner.sh" "$@"
