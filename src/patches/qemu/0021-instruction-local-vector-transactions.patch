diff --git a/target/riscv/helper.h b/target/riscv/helper.h
--- a/target/riscv/helper.h
+++ b/target/riscv/helper.h
@@ -10,6 +10,7 @@
 DEF_HELPER_4(mittens_sync_memory_instruction, void, i32, i32, i32, tl)
 DEF_HELPER_2(mittens_sync_instruction_fetch, void, tl, i32)
 DEF_HELPER_0(mittens_sync_instruction_fence, void)
+DEF_HELPER_0(mittens_sync_vector_memory_end, void)
 
 /* Mittens timed-memory ordering boundary */
 DEF_HELPER_0(mittens_sync_memory_fence, void)
diff --git a/target/riscv/translate.c b/target/riscv/translate.c
--- a/target/riscv/translate.c
+++ b/target/riscv/translate.c
@@ -1330,6 +1330,10 @@
 
     ctx->ol = ctx->xl;
     decode_opc(env, ctx, opcode16);
+    if (mittens_sync_instruction_fetch_timing_enabled() &&
+        is_mittens_vector_memory_instruction(ctx->opcode)) {
+        gen_helper_mittens_sync_vector_memory_end();
+    }
     if (is_mittens_vector_instruction(ctx->opcode, ctx->cur_insn_len) &&
         !is_mittens_vector_memory_instruction(ctx->opcode)) {
         gen_helper_mittens_sync_vector_instruction();
